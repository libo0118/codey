# Codey 内部开发文档

本文面向维护者，概括现有功能的实现方式和必要边界。用户功能介绍见 README.md，变更历史由 Git 保存。

## 整体结构

- Rust 主程序负责 Codex Electron 的启动、进程管理、配置、路由和本地数据维护。
- React 控制台构建后嵌入程序，经 CDP 挂载到 Codex 页面；完整界面按需加载，轻量脚本负责日常页面增强。
- `src/` 保存前端，`public/` 保存注入脚本，`backend/src/` 保存后端，`backend/resources/` 保存运行规则。
- `vendor/CodeyRuntime/` 提供应用发现、CDP、配置事务、会话存储和平台能力；FastCtx 作为配套程序分发。

## 启动与退出

启动先读取配置并恢复 Codey 临时状态；受控 Codex 停止后完成会话维护、模型目录准备和诊断保护，再建立路由、Hook 与运行配置，启动客户端并安装页面桥接。自动检查开启时，正常启动后静默检查更新，确认安装后沿正常退出流程停止 Codex、恢复配置再退出。启动失败时先清理并尝试适用的配置恢复，仍无法启动且清理成功、自动检查开启时通过原生窗口检查更新；未安装更新则保留原始启动错误，清理失败时不进入更新流程。两种检查均可由退出信号取消。

- 主进程优先使用 `NODE_OPTIONS=--require` 加载运行配置，Inspector evaluate 为回退；二者不同时使用。启动前检测 Electron fuse；NODE_OPTIONS 不可用且 Inspector 仍可用时改走 Inspector，否则使用包装器，必要配置无法确认时停止启动。控制台运行状态展示已确认的注入路径。Electron 对打包应用只保留 `--max-http-header-size`、`--http-parser` 两个 NODE_OPTIONS，`--require` 可能被直接丢弃：Windows 首次尝试未确认时，第二次尝试改走 Inspector 而不是重复 `--require`。Electron 单实例锁按应用而非安装路径生效，启动前停止所有 Codex 桌面实例（Store、独立安装、手动启动的都算）；等待期间新进程以退出码 0 退出视为单实例移交，清扫后再重试。
- Fuse 检测优先读取分离运行时 `chrome.dll`，该文件没有 wire 时回退主程序；macOS 读取应用内 Framework，缓存按实际运行时文件失效。Windows 启动时若 NODE_OPTIONS 与 Inspector 都不可用，启动器会在首次尝试前自动修复一次 fuse，成功即在本轮启动改用 NODE_OPTIONS；自动修复不请求提权，失败时保持 CLI 兼容模式。控制台修复按钮仅在已确认 CLI 兼容模式时启用，复用重启互斥与退出清理，停止 Codex 后修复并重新启动。普通进程保存原始字节和文件摘要、清除缓存；目标写入被拒绝时，经 UAC 启动仅处理固定运行时的助手，用备份/还原特权独占写入 NODE_OPTIONS 字节，不改 ACL 或所有者。助手在配置初始化前执行，校验路径、独占句柄和预期摘要，写后验证并在写入失败时回滚，父进程另行复读验收。自动尝试按运行时路径、大小和修改时间记录，同一运行时构建只尝试一次；就地更新使旧备份失效时，恢复被拒绝，启用则按当前运行时重新记录。取消授权或文件修复失败会尝试恢复普通权限启动，并通过原生对话框报告；成功只清除修复开始前已有的启动错误，后端明确拒绝的修复请求不会被前端当作等待确认。独立维护命令保留备份恢复能力，拒绝损坏或不匹配的备份。安装文件修改可能影响签名校验，不改变 Inspector 或完整性开关，重启后仍以执行标记确认注入成功。
- `--require` 启动环境关闭 Sparkle 运行期更新路径，更新仍由 Codey 的后台检查和用户操作处理；该变量只传给桌面主进程，不传给 app-server 包装器。
- 包装器调用应用内置 CLI，并检查配套工具宿主；通过认证握手或运行记录确认目标已执行。该确认不代表 app-server 已完成初始化。
- 辅助进程过滤环境变量时，包装器从已保存应用位置恢复 CLI。独立工具调用原样转发；受控 app-server 必须携带本次配置，禁止误入桌面启动及进程清理流程。
- Windows Store 按当前用户注册包定位，每次尝试按原 package family 刷新路径，激活后核验实际身份。Windows 启动环境显式携带准备配置时使用的绝对 `CODEX_HOME`，自定义目录无法传入 Store 进程时停止启动。包切换时先成功清理再重试，不跨正式版、Beta 或发布者切换。
- Windows 应用路径按目录判定：Store 包发现与手动选择共用启动器检查，跳过缺少有效启动器的包目录；选中的可执行文件只有在同目录存在 `ChatGPT.exe` 或 `Codex.exe` 时才视为安装目录，可执行文件名按 ASCII 大小写不敏感匹配；每次启动前确认该可执行文件仍存在，不满足时要求重新选择目录。第三方启动器（如 `codex-x.exe`）所在目录不会被当作 Codex 桌面应用，避免路径可归一化但进程无法启动。
- Codex 版本从应用内的包元数据读取；macOS 优先使用应用短版本，Windows 可回退到主程序的三段产品版本。读取失败时保留未知，平台包版本和 build 编号不参与应用版本推导。
- Windows 最多尝试两次，仅暂时性启动错误或已确认包切换允许重试。权限、配置、身份和清理失败直接报错；进程查询失败不能视为已退出或已清理。
- Store 配套文件暂存于 Codey 私有目录，通过清单、大小及复制时哈希校验复用。诊断记录阶段、进程身份和退出码，不记录令牌或完整配置。
- 宠物精简只调整对应开关，保留其他 JSON 值；主文件失败时尝试备份，仍失败则记录原因并继续启动。精简开启时，主进程跳过隐藏 avatar overlay 启动预热，并对隐藏浮窗恢复 `backgroundThrottling`；语音仍可按需创建浮窗。
- Windows 主进程拦截 Codex 周期进程采样 Worker（已知文件名、线程名，或同时具备 CIM/WMI、`Win32_Process`、`Win32_Perf*Process`、PowerShell 与 worker 消息的源码特征）。未知 Worker 只对不超过 2 MB 的普通文件检查源码，缓存随文件变化失效。一次性 `Win32_ComputerSystem` 厂商查询不在范围内；不经 renderer CDP 安装 WMI 探针。
- 可兼容设置热更新，启动参数、模型能力、上下文预算及会话错误重试次数等变化需要重启。重试次数通过当前 Provider 的运行时覆盖项传入，由 app-server 启动时读取，兼容本地路由和原生路由；当前 Provider 属于 Codex 内置 Provider 时跳过该覆盖，因为 Codex 会拒绝加载这种配置。前端等待超时只停止查询，不取消后台重启，可重新查询状态。

退出时先确认 Codex 停止，再关闭监视任务、回收进程、恢复临时配置，最后停止路由。等待 Windows 进程结束时持有原进程句柄，通过可取消的异步轮询检测退出，结束或取消后释放句柄；进程标识符已失效（句柄无法打开）按已退出处理，不作为监视失败。停止或恢复失败时保留已建立运行时供重试；最终退出的重试仍失败时，只补充限时回收本实例持有的直接子进程，并保留原始清理错误，不将局部回收视为整个进程树已停止。无法确认清理完成时可能需要人工退出残留 Codex。

## 配置与数据安全

- Codey 配置单独保存于系统配置目录，保留三份有效滚动备份；`CODEX_HOME` 非空时优先使用。账号刷新返回的配置按保存基线与当前草稿合并，保留未保存的用户设置，过期版本不覆盖新状态。
- Codex 配置默认只读；除用户主动执行的扩展管理外，仅维护明确属于 Codey 的路由恢复桩、临时 Hook、角色副本及遗留项。自定义 Provider 占用 Codex 内置标识时，Codex 会在加载配置阶段直接退出，启动前据此改名（追加 `-custom` 等后缀）并同步 `model_provider` 引用，结果写入运行日志；修复失败只记录原因，不阻止启动。持久数据修改使用所有权校验、快照复核、备份和原子替换。
- 语言迁移在 Codex 停止后按配置目录执行一次，只清除现有的简体中文覆盖值，使用配置事务保留其他字段并生成备份，成功后原子保存独立标记；迁移属于非关键维护，失败只记录真实原因并保留重试机会，不阻断本次启动。旧版未记录设置来源，因此首次迁移也会重置用户此前手动选择的简体中文；迁移完成后的所有语言选择均由 Codex 保存，启动参数和页面注入不再强制语言。
- 官方账号由 Codey 自行管理：通过与 Codex CLI 相同的 OAuth PKCE 流程（本地 1455 回调端口）登录，或导入 Codex 现有登录；每个账号以完整 `auth.json` 文档私有保存在配置目录的 `official-accounts` 下，可保存多个，仅一个为默认。设为默认时把该账号凭据写入 Codex 的 `auth.json`；Codex 自行刷新的令牌在读取列表和启动时回写到账号存储；同一账号缺少有效刷新时间时保留 Codex 当前凭据，有效时间按实际时刻比较，读取或解析失败时保留原文件并报告错误。移除默认账号时同步清除匹配的 Codex 登录，并在仍有账号时把最早添加的账号提升为默认；移除最后一个账号后，配置里由它派生的线路一并撤掉。
- 账号存储的读改写使用进程内互斥与文件锁；异步凭据结果仅在原记录仍匹配时提交，保留当前线路设置，不重新创建已移除账号。非默认账号刷新令牌使用所属线路的上游代理，代理失败不回退直连。
- 启动不再运行 `codex login status`：默认账号的登录态决定 Codex 客户端能否使用官方登录，存储账号的线路则各自携带凭据，与默认登录无关。存储为空而 Codex 已有 ChatGPT 登录时，会一次性收为默认账号，以兼容旧安装。
- Unix 私有配置、备份和日志限制当前用户访问；会话传输文件使用 0600、目录使用 0700。第三方密钥和通知凭据仍以明文保存在私有配置及备份中，部分字段会返回控制台供编辑。
- 普通错误日志只保存脱敏失败信息，不保存提示词、正常响应或认证值；官方账号额度刷新失败也记录原因，缓存返回不重复记录。请求日志的上游错误正文有单独边界，见后文。
- 控制台提供 Codex 配置检查与修复入口，Codey 配置加载失败时仍可调用并重新检查。用户确认后才执行修复：先核对实际配置目录与文件，必要时备份，只处理明确属于 Codey 的配置与可确认缺失项，完成后验证并提示从 Codey 重启 Codex 生效；无法安全处理的配置保持原样，失败原因写入脱敏错误日志，不自动重启 Codex。

## 线路与模型

本地路由使用不可变线路快照，每个请求固定自己的配置。线路列表按启用状态分组，同组保留保存顺序；启停先更新显示，失败后恢复，通过带配置版本校验的专用接口保存并更新路由、模型目录和子代理依赖，避免执行整套设置保存流程。官方账号与第三方凭据隔离，转发前还原模型标识（含路由提示头中的模型名）并移除 Codey 专用元数据，未改动的元数据保持客户端原始字节。

线路请求头在保存和发送前共用校验，名称不区分大小写，重复名称、非法值和超限配置会被拒绝。普通头先合并默认值与允许转发的客户端值，再应用线路覆盖；`null` 或空字符串表示删除，删除标记不会以空值头发送，认证和协议必需头保留专门约束。逐跳头及 `Connection` 声明的请求头不转发；Codey 内部标识只写入本地日志和下游响应，不随上游请求外发，自动生成的提示缓存键以普通 UUID 形态呈现。模型列表使用同一规则，已保存覆盖优先于来源 Provider 的请求头，并禁止自动重定向。

- 官方线路在启动和重启时按存储账号逐条派生，每个账号一条线路，分别使用所属账号的凭据，账号记录里的 `auth` 字段与 Codex 登录文档共用同一条读取路径；默认账号优先复用请求头携带的官方令牌，请求头无法解读或签发时间早于账号文件时改用后者，重新登录后仍在运行的 Codex 不会继续把内存里的旧令牌发往上游。线路名、短名称和上游代理保存在所属账号记录里，线路副标题显示所属账号邮箱。新账号和尚未保存过线路设置的账号按添加顺序补上默认名称「官方账号1」和「官1」，编号取当前未被占用的最小编号，所以移除账号后新增的账号不会和已有名称重复；已经保存的设置原样保留。默认账号排在第一位，也是写入 Codex `auth.json` 的登录身份；没有账号信息的请求（页头额度、审批复核）落到凭据文档就是 Codex 登录的那条线路，未设置默认账号时落到最早添加的账号，不受线路排序影响。官方线路的编辑入口只改线路名、短名称和上游代理，模型列举由同步入口进入。线路名最多 15 个字符，旧配置里更长的名称仍然可以加载，改名时才会被拒绝，短名称最多 2 个字符，编号越过 9 之后改用「官A」这类字母编号，官方与第三方线路共用一个命名空间，重复即拒绝保存，短名称作为模型名称前缀显示；派生时被第三方线路占用的短名称会改号并回写账号记录，面板显示的名称与线路列表、模型前缀保持一致。
- 账号失效标记只在官方明确拒绝凭据时写入：令牌端点返回 `invalid_grant`，或额度接口返回 401 而本地访问令牌仍在有效期内；网络故障、超时、限流和 5xx 只记请求日志。标记持久化在账号记录里，随账号摘要下发给界面，失效账号所在行显示红色底和失效标识，不提供切换默认入口，后端切换默认也会拒绝。失效账号不再尝试刷新令牌，额度查询直接返回本地原因，界面轮询跳过已标记的账号，只在失效状态变化时重算线路，因此官方接口不会因反复轮询被再次请求。线路派生跳过失效账号，标记写入后立即重算线路并热更新，所属线路随之从列表移除；存储账号全部失效时清空官方线路，不按缺少默认登录处理，默认账号被移除时由最早的有效账号补位。Codex 成功刷新该账号令牌或账号被重新添加后标记清除，线路重新出现。
- 选择器以带线路的稳定模型 ID 区分同名模型；已解析的页面 `turn/start` 使用上游原始模型名，并单独传递线路信息，线程状态请求沿用选择器 ID，使服务端回传的线程设置始终与模型列表一致。
- 模型别名历史不含凭据，支持线路变更后恢复旧任务；原始模型名可包含 `/`，历史别名仅还原一次。归属不明或存在歧义时拒绝请求，不猜测默认模型。
- 路由开启时，本机 app-server 使用本次 CLI 配置；新建、恢复和分支请求统一指定 `codey_router`，清除可覆盖供应商和端点的请求配置。主进程发送入口或 CLI 输入转发执行同样检查。改写共享请求出口时只替换负载与消息变量、保留打包代码原有的空值判断、旁路条件和调用形式，压缩器改用其它空值比较、可选调用或在空值判断旁追加条件仍然可用；连接访问器按名称识别，只有访问器本身改名才按漂移处理。锚点漂移时核对本次包装器、原始 CLI 和执行副本的实际路径及配置，将受控本机启动切换到 CLI 输入转发，并补齐被过滤的必要运行环境；保留其余子进程选项，不转发未知自定义命令。运行时配置和转发入口验证通过后允许降级并记录警告，配置缺失、校验超时或无法确认转发入口仍按启动失败处理。
- 路由关闭时沿用原生 Provider 和可用模型，历史任务在恢复入口转换；不批量改写 rollout 或 SQLite 中的历史 Provider。
- 模型目录优先复用可信本机元数据；本机模型缓存缺失、所有条目都缺少运行所需的指令内容，或已有快照早于本机 Codex 程序时，调用本机 Codex 命令行读取当前模型清单并保存为自有快照参与合并：单个模型优先采用带齐指令内容的条目，新快照可修复缓存里的同名残缺条目；只有覆盖全部固定官方模型且带指令内容的完整渲染才写盘，残缺渲染不占据快照位置；同一进程内每个 Codex 程序版本只重采一次，抓取失败释放该次占用以便重试，升级后重新抓取；快照不可得时沿用内置目录回退，仍无可用元数据才按缺少运行时目录处理。第三方能力不从无关模型继承。第三方线路的 Ultra 选项由模型目录和用户推理设置共同决定。第三方模型可声明思考强度档位与发送给上游的取值，未声明时沿用上游模板；声明只影响本地路由，官方线路拒绝此类声明。第三方线路同步到不完整清单时，仍被启用的模型保留在可用列表中，其上下文预算与思考强度设置不因此移除；同步失败或上游返回空清单不改动已保存的支持列表。官方模型清单只保留上游仍在提供的模型，模型下线后内置条目、旧选择与生成的目录一起移除，官方线路的带前缀条目不会因此阻塞整份目录；用户声明沿用官方模型名的第三方线路缺少本地运行时模板时仍按失败处理。空目录不传给 Codex；回退内置目录时保留用户目录，清理 Codey 旧引用。
- 模型增删可更新路由及页面列表，模型的思考强度声明随模型目录一并热更新；目录生成时在内存中应用上下文预算和思考强度后一次写入并校验，沿用旧目录时也合并应用，保留保存失败后的快照回滚。线路能力或上下文预算变化仍按实际运行配置提示重启。热更新先校验兼容性，再替换运行快照，待重启时保留最近已生效的模型目录，能力校验仍以启动配置为准。前端使用保存响应更新配置和模型，其余运行状态异步补充；同步和保存的诊断只记录阶段耗时。
- Images API 只向 OpenAI 兼容线路透明转发。网页搜索、自动审核和远程压缩须满足配置与模型能力条件。
- Fast 选项通过页面原生控件兼容显示和保存，覆盖直连及路由模式；选项可见不代表上游承诺加速。
- 杂事模型为空时，主进程标题补丁使用官方账号轻量模型，否则使用默认第三方线路的同名模型或默认模型，失败保留临时标题；有效选择通过各个主进程构建文件中的 Luna 常量应用于会话命名、Git 提交消息和环境建议，重启生效，补丁失败按文件记录且不报告为生效。模型解析只认当前已启用线路，失效选择保留供重新配置，不通过历史别名迁移供应商；直连模式只发送活动线路的上游模型名。自动复核仅在没有线路声明支持 `codex-auto-review` 时使用快照中预先解析的杂事模型，保存后生效，不可用则保持原有报错；复核请求不更改主会话或会话树的线路绑定。

## 协议转发与连接恢复

原生 Responses 保留同一线路的完整 reasoning 内容及加密状态，跨线路切换清理上一供应商的 reasoning 状态。HTTP 转发把上游端到端响应头（含 `x-codex-turn-state` 粘性路由令牌）原样透传给下游，仅重写传输层头；Chat Completions、Anthropic Messages 转换消息、工具、图片、用量及流式事件，并行工具调用产生的图片在该轮全部工具结果之后送出，保持调用与结果连续配对。目录只为原生 Responses 线路保留 `input_image.detail=original` 能力声明，兼容线路上的历史原图请求在转换时降级为 high。协议无法表达的必要内容在发送前拒绝，不静默裁剪历史。

- 线路可配置独立上游代理（http/https/socks5）：该线路的转发、模型同步及官方额度查询改走专用客户端，并禁用上游 WebSocket；其余线路仍遵循系统代理。
- 模型同步将上游思考档位存入独立的线路缓存，与手动声明分开；Responses 线路缺少元数据时补查同一模型端点的 Codex 目录。缓存只接纳已同步模型和受支持的档位，补查失败保留原模型列表及上次能力；运行目录、原生线路和子代理选择器共同使用缓存，手动声明优先。
- HTTP 入口使用 HTTP 上游；WebSocket 入口按线路能力、系统代理和退避状态选择传输，连接按线路、配置和认证身份隔离复用。上游 HTTP 客户端统一配置保活与连接池，HTTP/2 使用固定流量窗口而不启用自适应窗口：自适应窗口会把初始连接窗口压回 65535，使新版 h2 对小 DATA 帧的洪泛保护过早在逐 token 的 SSE 上触发。WSS 共享 TLS 配置与按服务端域名隔离的会话缓存，保留证书校验并禁用 0-RTT；服务端不接受会话恢复时执行完整握手。
- 只有握手或发送前失败可以回到同一线路的 HTTP 流式请求；请求一旦尝试发送，不自动重放、不跨供应商切换。
- 未知 WebSocket 能力同一身份只允许一个探测；不支持结果缓存一小时，其他失败按 5、15、30、60 秒退避。配置变化仅清理受影响线路并回收过时空闲连接。
- 原生 WebSocket 保存完整成功历史；下一轮断线续接时，先校验任务、身份、响应引用及工具配对，再展开完整输入。工具结果按 `call_id` 与调用配对，客户端自身注入、不带 `call_id` 的结果不参与校验并按原样转发；其余无法恢复的情况明确报错并带上具体条目，已发送的失败请求不参与重放。
- 近期历史仅存于路由进程内：共享缓存最多 64 个快照、16 MiB 估算大小、保留 5 分钟，按任务和身份隔离并计入共享预算；快照上限按会话计数，与单次请求携带的历史条数无关；不跨进程或机器恢复，也不保证保留所有分支。
- Chat 思考内容按标准 Responses 字段转递，下一轮还原 `reasoning_content`；缺失内容不补造。工具调用增量中的空类型按字段省略处理，明确的未知类型仍拒绝；工具调用、结果和终态继续校验顺序及完整性。
- 第三方原生 Responses 线路若被上游以缺少 reasoning 明文拒绝，Codey 为缺明文的项补一段占位文本后向同一线路重发一次；已有明文的项保持原样，官方线路和已发送的 WebSocket 请求不参与该重发。
- SSE 增量分帧并校验真实终态，提前断开不能伪装成功，每个请求最多尝试一个结束事件。已开始的 HTTP SSE 定期发送注释心跳，写入失败后停止上游等待。
- WebSocket 关闭可取消当前等待，后续应用消息有界排队；队列满时控制帧可能延迟。HTTP 合法半关闭不视为取消，响应头前或非流式等待仍依赖写入错误及超时。入口在区分两种协议前先探测请求行，空闲、半开或发出请求前已断开的连接按静默处理：尽力回复 408 后直接结束，不计为路由失败。
- 请求、解压输出、WebSocket 单消息及累计响应上限为 64 MiB；请求和历史分别受 256 MiB 估算预算约束。后台转换持有配额直至结束，预算不等于实际内存上限或 Token 窗口。
- 响应体读取统一使用 15 分钟总期限及空闲超时，压缩不再叠加请求总期限。上游 HTTP 错误保留状态码、脱敏信息及完整原因链，上下文超限保留可识别的结构化语义；错误正文读取超过总期限时与压缩路径一致返回结构化 504。

## 上下文与远程压缩

历史、Token 记账、压缩触发和结果提交由 Codex 管理；Codey 配置模型目录并检查压缩请求，不维护第二份可写会话历史。

- 第三方模型可声明窗口、自动压缩阈值和输出预留。预算按显式配置、精确官方元数据、未知模型保守值的顺序解析；未知模型使用 200000 Token、95% 有效比例。旧版本按 1M 能力写入的 1000000 Token 窗口会在首次读取时迁移为保守默认值。
- 输出预留保守投影到有效窗口，自动阈值不超过窗口的 90% 和扣除预留后的容量；这不限制实际输出长度，也不能扩大服务端容量。
- 预算变化要求重启；目录无法应用时回滚快照并报错。自定义预算缺少可用运行时目录时，启动、重启和模型保存都会询问用户是否恢复默认预算：启动或重启同意后清空预算并重新启动 Codex，保存同意后清空预算并继续保存本次改动，拒绝或无法询问时保留原预算并返回原错误。
- 关闭路由时保留预算但禁止编辑；官方模型入口不展示预算和思考强度声明，保存官方模型时保留已有相关配置。
- 旧 `/responses/compact` 和 V2 `compaction_trigger` 分别识别，并检查线路原生能力。同会话重叠压缩返回冲突，无会话标识时按输入摘要进行进程内在途去重；WebSocket 压缩转为 HTTP 前须恢复完整续接历史，无法恢复则拒绝发送。
- 压缩请求的响应头等待单独计时：流式压缩两分钟，非流式压缩与普通非流式请求相同；响应体读取沿用总期限与空闲期限，耗时的压缩不会在建连后的固定时长处被截断。等待超时与上游断流分别返回 504 与 502，并附带脱敏原因。压缩在校验完成前不向下游写事件，Codey 同时放宽运行时 Codex 客户端在该 Provider 上的流空闲期限，使其等待上限高于上游预算，避免客户端先判定连接失效。V2 完成事件省略输出时，从连续、无冲突的已完成输出项恢复候选；完整加密项使用响应大小上限，再检查完成状态及唯一非空压缩项。上下文超限返回 400；失败或取消释放锁，不自动重放。
- Chat/Anthropic 不接收原生压缩项；不可移植引用须先完整展开，否则拒绝请求。不会自造加密格式或删除压缩项后继续生成。
- 压缩期间固定线路快照。跨原生供应商的加密结果来源验证、独立摘要模型、语义质量、持久版本原子提交及二次摘要仍依赖宿主能力；当前检查不保证压缩后必然满足 Token 窗口。

## 控制台与页面增强

- CDP 负责桥接、注入及健康检查；仅确认桥接缺失时重建，页面忙或超时保持保守状态。用户脚本在同一文档成功执行后不因恢复重复运行；会话工具脚本安装失败或重载时释放旧监听、后台定时器和空闲回调，旧后台请求的结果不再更新页面或重新排程；已开始的业务操作仍完成必要的超时结算与资源清理。共享 DOM 观察器按订阅者各自的选项分发变更记录。额度、消息删除、列表刷新和完成态同步分别探测所需接口，失败后逐步延迟重试；接口缺失只停用对应功能，删除前确认卸载、恢复和刷新能力，探测超时后不执行迟到的删除。未知额度结构按不可用处理，保留已有后台快照。
- 前端使用 HeroUI、React Aria 和 Tailwind；内嵌界面通过 Shadow DOM 隔离样式，统一管理主题、焦点、弹层和提示挂载位置。HeroUI 样式按实际渲染的组件逐个导入，新增组件时需同步补充样式入口。
- 配置页通过菜单切换功能内容，共用统一页头，以平铺分区和列表组织设置；基础功能集中展示运行状态与功能策略。各模块首次访问时挂载，之后保持挂载，切换时保留草稿、加载结果和各页滚动位置；账号面板与线路卡片共用首次读取的账号列表；账号面板不挂载时，线路面板自行读取账号列表以显示所属账号邮箱。
- Shadow DOM 兼容包括样式变量映射、属性初始值及 React Aria 观察器生命周期补丁；内嵌界面优先读取宿主根节点已应用的主题标记与颜色方案，缺失时回退系统偏好，通过根节点属性和系统偏好监听同步设置页及弹层主题。依赖升级时核对上游修复情况。
- 模型选择器使用分组、搜索及虚拟列表，手动输入保留自定义模型；每个模型的上下文预算与思考强度声明合并为同一处模型设置。列表更新通过 QueryClient 发布，避免直接修改共享查询对象；扫描时只接纳同步可迭代的查询结果，远程代理对象不会阻断目录热更新。
- 线路页面提供脱敏显示开关：开启后统一隐藏线路 URL 的域名主体与账号邮箱的用户名部分，协议、端口、路径和邮箱域名保留。开关只存在于该组件的内存状态，不写入配置，菜单切换时保留，刷新或重新挂载组件后恢复明文显示。
- 页面补丁按功能锚点和语义定位，匹配失败保留原代码及失败状态；同一渲染器资源的补丁结果按内容指纹缓存，重载不重跑。主进程脚本复用 Node 原生加载器读取的源码，在编译前修改，避免重复读盘并保留模块加载语义；桌面遥测改写按语义追踪就绪值，压缩器把内联表达式提升为局部绑定时按绑定名继续识别。主进程补丁优先走 `NODE_OPTIONS=--require`，Inspector 为回退（二者不同时使用）；CLI 包装器负责 app-server 参数和本地路由输入转发。Inspector / `--require` 专属修改不能因 CLI 启动成功就宣称生效。

## Codex MCP 与 Skill 管理

功能由独立 Rust crate `codey-codex-extensions`、共用的扩展管理前端和宿主 `codex_extensions` 适配层组成，通过单一请求接口接入控制台。MCP 与 Skill 保留独立菜单和范围，共用筛选、分页、编辑器与确认流程；只在页面激活时读取，合并同时发起的清单请求并使用短时缓存，修改后使所有范围缓存失效。核心负责文件事务，单次清单扫描复用已读取内容；宿主注入用户路径与连接测试能力。

- MCP 读取用户或项目的 Codex TOML，保留未知配置；普通 Skill 清单发现用户、项目和系统资源。列表区分来源、配置有效性、启停状态与检查时间；依赖和反向引用来自当前范围的显式声明，不推断实际调用。项目 Skill 的路径启停规则写入用户配置，系统资源只读，含歧义的名称规则禁止修改。
- 插件缓存由独立入口按需读取，不参与普通 Skill 列表与批量操作。缓存内容只读，删除须确认且校验全部资源版本，仅处理缓存中可明确识别的独立 Skill 目录，拒绝链接、越界及包含其他 Skill 的父目录；不修改插件配置或托管记录。缓存仍可能被插件引用，删除可能影响后续加载。缓存请求与普通清单隔离，删除超时后等待原请求结束并刷新，才允许继续删除。
- 新增 MCP、创建与导入 Skill 默认禁用；MCP 的新建、编辑和导入统一使用 JSON，可从多服务配置中选择一项，导出为脱敏的标准 JSON；管理接口只接收 JSON，由服务端校验并转换为 Codex 原生配置，Skill 支持目录或 ZIP 导入和完整资源导出。导入拒绝链接、越界路径及超限内容，保留脚本的用户执行权限。外部安装不可直接编辑或卸载；托管的 SKILL.md 可按当前版本修复，其余资源或权限被外部修改时拒绝覆盖。
- 修改通过版本检查、文件锁、逐文件原子替换与复读校验提交；批量启停先完成校验再写入。启用、测试和删除要求确认。普通写入失败时，使用本次操作内存中的原始内容逆序恢复，拒绝覆盖外部变更；恢复不完整时提示检查当前配置。冲突保留草稿并要求比对最新版本，写入超时必须先刷新确认结果。不持久化历史备份，进程中断后不提供整批自动恢复；文件锁不能完全消除外部写入竞争。
- 编辑与导出过滤配置注释并以占位符保留凭证、敏感命令参数及未知字段，新增凭证优先使用环境变量引用；Codex 要求的普通配置仍保存在原位置。审计只记录操作和状态，不记录配置内容。
- MCP 测试需确认当前配置版本，只执行 stdio 或 Streamable HTTP 握手和工具清单检查，有超时及自有进程清理，不调用业务工具、不复用 Codex OAuth。Skill 检查限于元数据与资源路径，不执行脚本或证明依赖可用。保存返回重启提示，不自动打断会话，最终生效仍受 Codex 版本和启动覆盖影响。

## 会话、插件与提示词

- Codey 原生插件独立于 Codex 插件市场，使用版本化 C ABI 和 Rust SDK。插件包经路径、大小、平台、版本、配置模板及制品摘要检查后安装到独立版本目录，检查和安装不执行代码；用户启用后才加载，并在后续启动时恢复；启动加载失败会持久停用并保留错误，等待用户重新启用。每个插件在用户状态目录中独立保存版本制品、持久数据和日志，SDK 通过初始化上下文接收绝对路径，兼容旧插件入口。升级保留数据，卸载默认保留配置、数据和日志，未完成退出的实例会阻止卸载；日志按大小轮转。控制台集中管理导入、启停、配置、升级、卸载和目录查看；列表状态未知时暂停修改，重新启用失败后重读实际状态。安装记录与配置分别原子保存。
- 当前扩展点为本地路由认证后的请求头处理，只传递插件声明的字段与必要元数据，不传递请求体或认证凭据。修改结果整体验证后应用，回调错误使当前实例退出活动集合，业务请求继续。无活动插件时直接跳过扩展处理。
- 原生插件属于可信进程内代码，权限声明不构成沙箱，摘要校验不证明发布者身份。实例调用串行化，已启用实例固定版本、配置及权限，变更在重新启用后应用；在途调用持有实例引用，退出时关闭实例，动态库映射保留到进程结束。Windows 删除曾加载的 DLL 可能需要重启。
- 插件统一使用版本目录外的 `config.json`，包内同名文件只提供初始模板，缺省为空对象；旧状态中的配置仅在文件缺失时迁移。配置编辑器按 JSON 结构展示字段，上方说明使用灰色小字且不可选中或编辑；标量及不含对象的值数组可直接编辑，值数组支持增删元素，含对象的数组逐项修改字段值；保存按原文位置替换值，保留字段、注释与排版，并校验 JSON 对象、类型和大小。任意对象中的 `_comments` 必须是字符串映射，同级说明优先，祖先的点分隔字段说明可用于嵌套项；宿主从运行配置中移除说明，仅改说明不要求重新启用，其他业务约束由插件初始化校验。旧 Schema 和 HTML 元数据仅为安装兼容而读取，不再生成或加载插件提供的配置页面。保存前复核内容摘要并原子替换，发现外部修改时保留草稿并要求重新读取，该检查不能完全消除外部进程写入的竞争。读取或解析失败时禁止保存，未保存退出和重新读取需确认，保存期间禁止重复提交和退出，过时响应不覆盖新编辑状态。配置保存在当前用户私有目录，尚无独立密钥存储和第三方依赖解析。

- 会话导入、导出、删除轮次和恢复备份先释放目标会话，再校验大小、并发变化和路径；无法稳定确认目标时拒绝删除。侧边栏的永久删除图标不经过后端，而是从会话行的菜单定义里取出 Codex 官方的删除项并触发它，取不到时回退为该行的右键菜单，删除范围与确认流程由 Codex 决定。
- 删除轮次写回前复核文件身份与内容，先持久保存私有恢复记录，再在原文件上修改，以保持现有追加句柄有效。中断写入可在后续删除或启动维护时恢复；遇到无法确认的外部修改保留恢复记录并报错。墓碑引用检查采用有界流式扫描，文件超限或读取失败时保留删除意图。导入提交在进程内串行执行，先验证会话元数据与索引身份一致，失败时只清理本次创建的文件。
- 会话索引及元数据读取已有 SQLite 和 rollout，发现缓存随文件替换、删除和 WAL 变化失效；启动期写入只在 Codex 停止后执行。
- 页面仍显示运行中时，完成态校准会复核原生任务状态并通过原生恢复流程重读；校准期间切换任务后补查当前任务。该流程不校验已经中断或空闲任务的历史完整性，索引时间也不能作为消息完整性的依据。
- 插件市场分别读取安装与启用状态，配置读取失败会报错。市场修复校验索引及本地插件清单，仅替换托管快照并保留旧目录，不覆盖自定义来源；读取状态不触发修改。页面兼容补丁只识别插件接口及请求封装，不扫描聊天正文；Computer Use 沿用官方统一插件和 `cua_repl`。主进程在生成插件配置时使用缓存的兼容副本，同步延长浏览器策略初始化和网络请求期限，保留身份及策略校验；旧版把插件入口指向副本，新版改写插件环境里声明的浏览器服务路径，两者都按实际加载的实现生成副本，原始安装文件不变，结构不匹配时保留官方入口并记录原因。
- 提示词优化使用运行中的路由或独立配置，后端校验地址、认证和模型。Codey 路由按所选线路加载账号凭据，官方请求关闭存储并省略不支持的输出上限参数，不依赖默认账号登录状态。Codey 路由统一请求流式响应，解析 SSE 的同时接受忽略流式标记后返回的单个 JSON 正文，兼容只在流式请求下回填正文的第三方线路。成功 HTTP 响应正文无效时直接报错并说明上游返回的实际形态，仅明确 404 保留兼容端点处理。

## 子代理与 FastCtx

- 增强在原生 macOS、Windows 上生成五种 Codey 角色和内部默认角色，复用 Codex agents 工具及生命周期 Hook；控制台分别配置默认角色与五种专用角色的模型和思考深度，默认角色同步兼容字段；提示鼓励尽早委派有明确收益的独立任务，不以文件数或工具次数限制派发。完整匹配旧版默认内容的根提示在运行时替换为当前规则，用户自定义源文件保持原样；角色推荐不替代权限检查。
- Hook、角色副本和策略仅写入本次运行资产；Hook 状态锁与编排账本锁按会话目录隔离，跨会话只读取原子写入的账本快照。只读角色最多并行三个，出现写入角色时最多两个，由根代理在所有尝试结束后验收。
- 子代理沿用对应线路的模型和思考深度，不按官方模型名单限制第三方模型；自定义目录需包含每个启用角色的实际模型标识，内置目录回退要求原始模型只对应一条线路。实际可用性仍取决于 Codex、协议与上游能力。启动校验失败只关闭本次增强并保留设置，错误指出具体角色和模型，热更新失败保留原运行配置。
- 内置目录模式下，启动已启用增强则线路与模型映射在重启前固定；角色模型、深度和排序仍可在映射不变时热更新。
- 门禁结合任务绑定、能力证明和账本限制工具，策略缺失或身份不明时保守拒绝。发起中止前先撤销目标工具权限，成功回执或终态核对后才释放写入预留，并保留身份绑定以拒绝排队任务恢复权限；Codey 不清空 Codex 原生任务队列。支持等待、完整列表和单目标状态核对，仅完整无筛选列表可全量核对状态。根调用、用户输入和会话结束共用有时限的恢复流程；工具明确未注册时使用较短等待期，可用状态回复会取消该计时。恢复先撤销遗留任务的工具权限，按失联处理并保留诊断，损坏账本隔离保存；等待超时不视为任务成功。
- 只读 SQL 使用保守词法检查，仍须数据库只读账号；Hook 和角色名不构成操作系统沙箱。跨 app-server 的写入互斥仍需调用方安排。
- 视觉角色获得对应图像及界面工具能力，协议回退保留图像块。协作任务正文中的明文在转换前恢复为可见文本；加密令牌由原生 Responses 保留，Chat Completions 和 Anthropic Messages 在发送前明确拒绝。Codey 不尝试本地解密；正文不可读时子代理停止执行，主代理只重述一次，仍失败则接管。验收按任务及尝试核对实际修改与清理结果。
- 内置 FastCtx 提供文件读取、搜索、发现和批量替换；已有服务不重复注册，使用进程覆盖加载，不写入用户配置。监督进程在读取到客户端 EOF 后限时转发剩余输出，超时终止并回收所属 worker；持续输出和管道阻塞不延长期限，共享运行服务沿用 FastCtx 自身的生命周期管理。

## 请求日志、用量分析与额度估算

- 日志默认关闭，可热启停；主路径有界、非阻塞观测，独立线程写 SQLite，保留 NDJSON 调试输出。过载允许丢记录，不阻塞模型请求，不作为计费或审计账本。系统浏览器中的日志页通过链接接收打开时的 Codex 主题，刷新时保留；旧链接缺少主题时跟随系统外观。
- 日志记录线路、模型、状态、上游传输、阶段耗时、Token、服务档位和请求体形态摘要，官方线路额外记录所属账号；旧库缺少这些列时查询照常返回，写入端在打开时补列。模型分请求模型与上游回报的实际模型两项，前者是最终发往上游的模型，后者取自上游响应的 model 字段，桥接线路在适配器解析响应时提取，原生线路由原始响应字节投影得到，上游没有回报时留空。请求体只保留 input 的形态（缺失、空值、字符串、对象、数组）、input 项数、是否带 previous_response_id 和字节数，客户端请求与最终发往上游的请求分别记录，用于区分请求本身为空与中间改写后才为空。正常提示词、响应与凭据不落盘。请求头与上游响应头分别记录最终结果，仅公开安全字段的值，会话 Cookie 一类响应凭据同样只保留占位符；HTTP 传输层自动生成的字段不在记录内，WebSocket 在新建握手时记录请求头与握手响应头。错误正文脱敏后最多保留 64 KiB，仍可能含供应商回显的请求片段；协议适配的流式错误在结束记录前保留脱敏、限长的具体原因，Chat 结果转换失败同时记录上游结束原因。
- 列表连续翻页复用时间和请求 ID 游标，跳转到未缓存游标的页时按页码查询，筛选变化时同步重置分页与游标，筛选范围固定，筛选与统计可按官方账号区分；统计使用独立只读事务汇总和分桶，不受当前页影响。模型候选按时间、供应商与账号独立去重并按游标分页读取，不复用排名统计，也不重复计算总览。缺失用量保留未知，缓存及推理子项不重复加入总 Token。
- 用量分析、请求日志入口与官方账号管理仅在开启本地路由时于控制台“线路与模型”展示。用量分析在控制台主体内切换并保留设置与返回位置，复用请求日志聚合接口；支持近 7 天、近 30 天及全部保留日志，全历史从最早匹配记录开始。时间趋势默认采用 UTC 逐日热力图，可悬停查看摘要并切换原直方图；每日汇总与概览使用相同过滤和只读快照，长历史按年展示，直方图仍按跨度调整分桶。模型聚合按 Token 排序再截断，前端展示前 12 项并将其余合并为“其他”。统计不包含绕过 Codey 的直连请求；未上报用量、采样和记录异常单独提示，失败可重试，过期响应不覆盖新范围。
- SQLite 使用单写入线程、批量事务和 WAL；批次有限重试并按请求 ID 去重，保留清理分批执行。健康状态暴露采样、待写入、写入错误和丢弃数，备份须取得一致副本。
- 官方额度按账号读取，每条线路查询所属账号的凭据用量，优先账号用量，失败回退通用额度，不合并模型专属额度。成功缓存共享 60 秒，按凭据文件分别保存，最近使用的 24 条凭据缓存留在内存中，超限时只淘汰最久未使用的一条，账号凭据变化后按文件指纹失效；有效周快照可在刷新失败时标记过期使用。
- 周限估算按官方账号分别进行：每个账号只统计自己周期内的日志，界面通过下拉框按账号独立切换展示，仅包含有明确账号请求记录的官方账号；使用各账号官方已用比例推算总限额，不合并其他账号请求或额度，当前速度整周消耗单独计算。比例为零、周窗无效或缺失时不猜测结果。
- 费用按模型、实际档位和长短上下文独立计价，明确请求档位次之，未记录档位使用 Standard 并标注推定；未知价格不纳入金额合计。
- 缓存读写按输入子集计费，缺失写入量单独提示；GPT-6 Astra 缓存读取使用自定义双倍单价。工具、存储等不完整计量未计入，地区不另加价，估算不代表订阅实际扣费。
- 账号额度查询串行执行，估算页合并同账号的在途读取，强制刷新在普通读取结束后执行，避免复用旧结果；估算完整分页后才提交，关闭时停止后续读取；日志可能缺失，官方额度可能包含其他设备用量，跨页读取也不是事务快照。

## 通知、诊断与更新

- 飞书、企业微信、Telegram、ntfy 和微信 ClawBot 共用任务状态及去重机制，最多保存 32 个渠道；送达状态不明时不盲目重发，除超时与限流外的 4xx 视为配置错误直接结算，不再自动重试。ntfy 向服务器根地址发布 JSON 请求，主题必填、访问令牌可选，保存后访问令牌不再返回渲染进程。
- 等待介入同时识别同步、异步提问；异步 accepted 只表示问题已发布，按实际回答清除等待，失败或终态也会释放状态。
- Trace 与 Crashpad 分别按设置执行保护和手动清理，复用互斥锁、文件白名单及静默期保护，不处理会话或账号数据；清理失败不将缺失统计显示为零。启动各阶段耗时（更新检查、默认账号解析、模型同步、运行时各步）以毫秒写入诊断日志。
- 自动检查设置默认开启，统一控制启动检查和页面定时检查；控制台确认关闭风险后更新草稿，保存后同步到页面脚本，停止后续检查并忽略在途自动结果，手动检查仍可用。旧配置缺少该设置时默认开启，旧客户端保存时缺少该字段则保留原值。更新检查读取发布清单，与默认官方账号解析并行进行，安装前走统一退出清理；安装后由独立助手重启主程序，新写入的程序文件仍被占用时在有限时间内重试等待释放。构建将前端及注入脚本嵌入 Rust 程序，内容未变时保留产物时间戳以复用增量编译；平台打包及发布由现有脚本和工作流负责。
