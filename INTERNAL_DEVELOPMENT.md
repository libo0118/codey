# Codey 内部开发文档

本文说明核心实现与维护边界，用户功能见 [README.md](README.md)。

## 整体结构

- Rust 主程序负责 Codex Electron 启停、配置、路由和本地数据维护；React 控制台与页面脚本嵌入程序，通过 CDP 注入。
- `src/` 为前端，`public/` 为注入脚本，`backend/src/` 为后端，`backend/resources/` 为运行规则。
- `vendor/CodeyRuntime/` 提供应用发现、CDP、配置事务、会话存储和平台能力；FastCtx 随程序分发。

## 启动与退出

- 启动先恢复临时状态，停止受控 Codex 后维护会话、准备模型目录，再建立路由、Hook 和运行配置，启动客户端并安装页面桥接。
- 主进程注入优先使用 `NODE_OPTIONS=--require`，Inspector 为回退，二者互斥；Windows Store 优先使用 Inspector。CLI 包装器调用应用内置 CLI，传入本次配置并确认实际执行；CLI 启动成功不代表主进程补丁或 app-server 初始化成功。
- 启动前检查 Electron fuse，独立安装按需修复；Store 安装不自动修复。修复校验运行时身份、摘要和备份，写后复读，失败回滚；需要提权时只修改固定运行时的目标字节，不改 ACL、所有者或其他安全开关。修改可能影响签名，最终仍以执行标记确认注入。
- Windows 通过安装布局识别 Codex，避免误停同名 CLI。Store 按当前用户注册包发现并核验身份，不跨发布者或版本渠道切换；自定义 `CODEX_HOME` 无法传入时停止启动。仅暂时性错误或已确认包切换允许有限重试，权限、身份、配置和清理失败直接报错。
- 宠物精简保留其他配置，跳过隐藏浮窗预热并恢复后台节流；语音浮窗仍可按需创建。Windows 仅拦截已识别的周期进程采样 Worker。
- 支持热更新的设置直接应用；启动参数、模型能力、上下文预算和会话错误重试次数等要求重启。前端等待超时不取消后台重启。
- 退出依次停止 Codex、监视任务和所属进程，恢复临时配置，最后停止路由。无法确认停止或恢复完成时保留状态供重试，不把局部进程回收视为清理成功。

## 配置与账号安全

- Codey 配置独立保存，保留三份有效滚动备份，优先使用非空 `CODEX_HOME`。异步结果按保存基线合并，避免覆盖未保存草稿或较新状态。
- Codex 配置默认只读；除用户主动管理扩展外，仅维护 Codey 所属临时配置、恢复项，以及与内置 Provider 标识冲突的自定义名称。持久修改使用所有权校验、快照复核、备份和原子替换。
- 官方账号使用 Codex CLI 同源 OAuth PKCE 登录或导入现有登录，每个账号私有保存完整凭据；默认账号同步到 Codex 登录文件，Codex 刷新的令牌回写账号存储。读改写由进程内互斥和文件锁保护，过期异步结果不能重建已删除账号。
- 非默认账号使用所属线路代理刷新令牌，代理失败不回退直连。仅明确凭据失效时标记账号并移除其派生线路；网络故障、限流和服务端错误不判为失效。重新登录或有效刷新后恢复线路。
- Unix 私有配置、备份和日志限制当前用户访问。第三方密钥及部分通知凭据仍以明文保存在私有配置和备份中；普通错误日志脱敏，不保存提示词、正常响应或认证值。
- 配置修复需用户确认，只处理可确认属于 Codey 的配置和缺失项；先备份、后验证，无法安全处理的内容保持原样，完成后提示从 Codey 重启。

## 线路与模型

- 每个请求固定不可变线路快照，官方账号与第三方凭据隔离。每个有效官方账号派生一条线路，使用所属凭据；默认账号决定 Codex 登录身份，无账号信息的请求据此选择线路。
- 带线路的稳定模型 ID 区分同名模型，转发时恢复上游模型名并移除 Codey 元数据。历史别名支持恢复旧任务；归属不明或有歧义时拒绝请求，不猜测默认模型。
- 请求头在保存和发送前统一校验，过滤逐跳头与内部标识；线路覆盖可删除普通头，认证和协议必需头受单独约束。模型同步使用相同规则并禁止自动重定向。
- 路由开启时，受控 app-server 的新建、恢复和分支请求统一使用本地 Provider，清除可覆盖供应商及端点的请求配置。主进程补丁不兼容时，仅在运行配置和 CLI 输入转发均验证通过后降级，否则启动失败。关闭路由后恢复原生 Provider，不批量改写历史数据。
- 模型目录优先使用可信本机元数据，缺失、残缺或落后于 Codex 版本时调用内置 CLI 补全，自有快照不可用时回退内置目录。第三方能力不从无关模型继承，缺少必要运行时模板时明确报错，空目录不传给 Codex。
- 第三方模型同步失败或返回空清单时保留已有支持列表；不完整清单保留仍启用的模型及设置。官方目录移除上游已下线模型。模型增删和思考强度声明可热更新，能力与预算变化仍按运行配置要求重启；更新失败回滚快照。
- 上游思考档位按线路独立缓存，与手动声明分开；Responses 线路元数据不足时补查 Codex 模型目录，只接纳已同步模型和受支持档位，补查失败保留上次结果。运行目录、原生线路和子代理选择器共同使用这些能力，手动声明优先。
- Images API 仅向 OpenAI 兼容线路透明转发；网页搜索、自动审核和远程压缩受配置与模型能力限制。Fast 沿用原生控件，可见不代表上游保证加速。
- 杂事模型用于标题、提交消息和环境建议，重启生效；自动复核仅在没有线路声明专用模型时使用已解析的杂事模型，保存后生效，不改变主会话线路绑定。

## 协议转发与恢复

- 原生 Responses 保留同线路 reasoning 与加密状态，跨线路时清理上一供应商状态。Chat Completions、Anthropic Messages 转换消息、工具、图片、用量和流式事件；流式工具增量的 legacy 与索引式形状只在同一次调用上合并，没有内容的空槽位丢弃；无法表达的必要内容在发送前拒绝，不裁剪历史或补造思考内容。
- HTTP 透传端到端响应头。线路可独立配置 HTTP、HTTPS 或 SOCKS5 代理，用于转发、模型同步和额度查询，并禁用该线路上游 WebSocket；其他线路遵循系统代理。
- 连接按线路、配置和认证身份隔离复用；HTTP/2 使用固定流量窗口，WSS 保留证书校验并禁用 0-RTT。WebSocket 能力探测去重，缓存不支持 WebSocket 的探测结果，其他失败退避。
- WebSocket 仅在握手或发送前失败时回退同线路 HTTP；一旦尝试发送，不自动重放或跨供应商切换。第三方原生 Responses 因缺少 reasoning 明文被拒绝时，可补占位文本后向同线路重发一次，官方线路及已发送的 WebSocket 请求除外。
- WebSocket 续接先校验任务、身份、响应引用和工具配对，再展开成功历史。历史只存在路由进程内，按任务及身份隔离，共享最多 64 个快照、16 MiB、5 分钟，不保证保留全部分支。
- SSE 校验真实终态，提前断开不能视为成功；流式心跳写入失败后停止上游等待。HTTP 半关闭不直接视为取消，其他等待受写入错误和超时约束。
- 请求、解压输出、单条 WebSocket 消息和累计响应上限为 64 MiB；请求与历史分别受 256 MiB 估算预算约束，不等同于实际内存或 Token 窗口。响应体受总期限与空闲超时约束，错误保留状态码及脱敏原因。

## 上下文与远程压缩

- 历史、Token 记账、压缩触发和结果提交由 Codex 管理；Codey 配置模型目录并校验请求，不维护第二份可写历史。
- 第三方预算依次采用显式配置、精确官方元数据、未知模型保守值。输出预留与自动压缩阈值约束有效窗口，不能扩大服务端容量或限制实际输出长度。预算变化需重启；缺少运行时目录时，用户确认恢复默认预算后才能继续。
- 旧压缩接口与 V2 压缩分别识别并检查原生能力，同会话压缩互斥。WebSocket 转 HTTP 前必须恢复完整历史；Chat/Anthropic 不接受原生压缩项，不可移植引用须先展开。
- 压缩使用独立等待预算，完成状态与唯一有效压缩项验证通过后才下发事件；失败或取消释放锁，不自动重放。压缩期间固定线路快照，结果质量、持久提交和最终是否满足 Token 窗口仍依赖宿主。

## 控制台与页面增强

- CDP 负责桥接、注入与健康检查，根据故障阶段重试或重建，睡眠唤醒后留恢复宽限期。同文档不重复执行已成功的用户脚本；脚本重载释放旧监听和定时器，过期请求结果不再更新页面。
- 各页面能力独立探测，接口缺失只停用对应功能；删除操作必须先确认卸载、恢复和刷新能力，超时探测不能触发迟到的删除。
- 前端使用 HeroUI、React Aria 和 Tailwind，Shadow DOM 隔离样式，统一管理主题、焦点与弹层。组件样式按需导入，依赖升级需检查 Shadow DOM 兼容补丁。
- 配置模块首次访问后保持挂载，保留草稿、数据和滚动位置。模型选择器支持分组、搜索及虚拟列表，通过 QueryClient 更新共享数据；线路脱敏开关仅保存在组件内存。
- 页面与主进程补丁按功能锚点及语义匹配，失败保留原代码并记录状态；补丁结果按内容缓存。主进程在原生加载器编译前修改源码，避免改变加载语义。

## Codex MCP 与 Skill 管理

- 独立 Rust crate `codey-codex-extensions` 处理文件事务，共用前端与宿主适配层提供路径和连接测试。清单按需读取、合并并发请求并短时缓存，修改后使缓存失效。
- MCP 读写用户或项目 TOML，保留未知配置，编辑和导入统一接收 JSON。Skill 支持用户、项目和系统范围、目录或 ZIP 导入及完整导出；系统资源只读，插件缓存单独按需读取，不参与普通批量操作。
- 导入、删除校验路径、大小和资源版本，拒绝链接、越界及包含其他 Skill 的父目录。外部安装不能直接编辑；插件缓存删除只处理可明确识别的独立 Skill，可能影响后续插件加载。
- 修改使用版本检查、文件锁、原子替换及复读校验；批量操作先校验再写入，失败逆序恢复且不覆盖外部变更。启用、测试和删除要求确认；冲突保留草稿，写入超时先刷新确认结果。无持久事务备份，进程中断后不保证整批恢复。
- 编辑及导出以占位符保留敏感值，新增凭证优先用环境变量；审计仅记录操作与状态。MCP 测试只做握手和工具清单检查，不调用业务工具、不复用 Codex OAuth；Skill 检查不执行脚本。
- MCP 修改后通过受控 app-server 通道通知 Codex 重读，必要时回退页面桥接；刷新失败保留保存结果。已加载会话下一轮生效，Skill 保存仍提示重启确认。

## 原生插件

- Codey 原生插件独立于 Codex 插件市场，使用版本化 C ABI 和 Rust SDK。安装只校验包、平台、版本和摘要，用户启用后才加载；加载失败停用对应插件，不影响其他插件。
- 每个插件独立保存制品、配置、数据和轮转日志；日志大小随列表读取，确认清理后在共享文件锁内清空当前日志与备份。升级和卸载默认保留配置、数据及日志；在途实例阻止卸载，Windows 已加载 DLL 可能要重启后才能删除。
- 请求生命周期固定插件实例快照，按插件 ID 顺序调用，允许发送前或响应头阶段继续、等待、终止和受限重发。宿主限制回调、等待与重发次数，正文下发后不重放；启用生命周期插件的 Responses 上游使用 HTTP/SSE。
- 插件只接收必要元数据及声明的请求头、响应头，官方认证需额外权限。插件与协议回退共享重发额度，结束通知只表示传输结果，不解释业务正文。
- 原生插件是可信进程内代码，权限声明不构成沙箱，摘要不证明发布者身份。版本、配置和权限固定到本次实例，变更需重新启用，动态库映射保留到进程退出。
- 配置基于安装包 JSON 模板，按原文位置替换值，保留注释和排版；保存前校验类型、大小与内容摘要。说明字段不传入运行配置，外部冲突保留草稿。当前无独立密钥存储或第三方依赖解析。

## 会话、插件市场与提示词

- 会话导入、导出、删除轮次及恢复先释放目标会话，再校验路径、大小和并发变化。删除先保存私有恢复记录，再修改原文件以保留追加句柄；无法确认外部变化时保留记录并报错。导入失败只清理本次创建文件。
- 索引及元数据读取 SQLite 和 rollout，缓存随文件及 WAL 变化失效，启动期写入仅在 Codex 停止后执行。侧边栏永久删除调用 Codex 官方菜单，删除范围与确认由 Codex 管理。
- 完成态校准复核原生任务状态并通过原生恢复重读，不保证中断或空闲任务的历史完整性。
- 插件市场分别读取安装和启用状态；修复仅替换托管快照，不覆盖自定义来源。浏览器兼容使用按实际实现生成的缓存副本，保留身份和策略校验，不改原始安装文件；结构不匹配时保留官方入口。
- 提示词优化使用当前路由或独立配置，后端校验地址、认证和模型，按所选官方线路取凭据。流式解析兼容返回单个 JSON 的上游，无效成功响应明确报错。

## 子代理与 FastCtx

- 原生 macOS、Windows 生成五种专用角色及内部默认角色，复用 Codex agents 工具和生命周期 Hook。角色模型与思考深度沿用对应线路，不用官方模型名单限制第三方；实际能力取决于 Codex、协议与上游。
- Hook、角色副本和策略仅写入本次运行资产，账本按会话隔离。只读角色最多并行三个，有写入角色时最多两个，全部尝试结束后由根代理验收。启动校验失败只停用本次增强，热更新失败保留原配置。
- 工具权限结合任务绑定、能力证明和账本判断，身份不明或策略缺失时拒绝。中止先撤销工具权限，确认终态后释放写入预留，排队任务不能恢复权限；失联恢复保留诊断，超时不视为成功。Hook 不构成系统沙箱，跨 app-server 写入互斥仍由调用方安排。
- 协作正文中的明文在协议转换前恢复；加密令牌仅由原生 Responses 保留，兼容协议发送前拒绝，不尝试本地解密。正文不可读时子代理停止，主代理完整重述一次，仍失败则接管。
- FastCtx 提供文件读取、搜索、发现和批量替换，通过进程覆盖加载，不写用户配置、不重复注册。监督进程在客户端关闭后限时转发剩余输出并回收所属 worker。

## 日志、用量与额度估算

- 请求日志默认关闭，可热启停；有界、非阻塞采集，独立线程批量写 SQLite WAL，并保留 NDJSON 调试输出。过载可丢记录，不阻塞请求，不作为计费或审计账本。
- 记录线路、账号、模型、状态、传输、耗时、Token 和请求形态；正常提示词、响应及凭据不落盘，请求头只公开安全字段。错误正文脱敏后最多保留 64 KiB，仍可能包含上游回显的请求片段。
- 列表按游标分页，统计用独立只读事务，不受当前页影响。用量分析复用聚合接口，缺失用量保留未知，缓存与推理子项不重复计入总量；仅覆盖经过 Codey 且成功记录的请求。
- 官方额度按账号凭据查询并缓存，凭据变化后失效，刷新失败可标记使用过期周快照。周限估算只统计对应账号周期内的日志，用官方已用比例推算；比例、周期或数据缺失时不猜测。
- 费用按模型、服务档位、上下文及缓存独立计价，缺失档位按 Standard 并标注推定，未知价格不计入金额。工具和存储等未完整计量，估算不代表订阅实际扣费；日志缺失、其他设备用量及跨页变化均可能影响结果。

## 通知、诊断与更新

- 飞书、企业微信、Telegram、ntfy 和微信 ClawBot 共用任务状态与去重，送达不明时不盲目重发。等待介入覆盖同步和异步提问，按实际回答、失败或终态清除状态。
- Trace、Crashpad 保护和清理使用互斥锁、文件白名单及静默期，不处理会话或账号数据。诊断记录启动阶段和注入失败信息，不记录令牌或完整配置。
- 自动检查默认开启，统一控制启动与页面检查；关闭后忽略在途自动结果，手动检查仍可用。启动失败仅在清理成功且自动检查开启时尝试更新。
- 安装由独立更新助手接手，主程序先完成统一退出清理；助手确认目标目录版本替换成功后才重启，失败原因留给下次启动显示。构建嵌入前端和注入脚本，现有脚本及工作流负责打包发布，并校验内嵌版本与清单一致。
