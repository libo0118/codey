# AGENTS.md

## 文档约束

- 两份文档各有侧重：`README.md` 回答使用者能做什么，`INTERNAL_DEVELOPMENT.md` 回答维护者现有功能如何实现。
- `README.md` 以简短的功能介绍为主，每项通常一句话，覆盖控制台和 Codex 页面内的主要能力；仅补充必要的使用方式与注意事项，不展开操作细节、界面布局或异常处理分支。
- `README.md` 不写构建命令、发布流程、依赖版本、端口、协议、数据库结构、文件路径、内部模块名或实现原理；保留必要的第三方声明。
- `INTERNAL_DEVELOPMENT.md` 按功能或模块概括当前实现，只保留核心流程、关键依赖和必要的安全与兼容边界，不逐项罗列函数、字段、参数或代码细节。
- 两份文档均不作更新日志：不追加日期流水、版本改动、修复经过、测试结果、性能测量记录或待实施方案；历史信息交由 Git 保存。
- 功能调整时直接改写对应段落，合并重复内容并删除过时描述，不在文末不断追加说明，也不把从 README 删去的细节全部搬入内部文档。
- 优先使用简短、自然的中文，直接说明能力、流程或限制；删除背景铺垫、宣传用语、重复解释和无关示例，避免写成完整操作手册或开发发布手册。
- README 每项功能原则上只写一句话，不在一句中堆叠子功能；新增能力优先并入已有分类，只有独立的主要能力才新增条目。
- 内部文档每个条目只说明一个核心机制或必要边界；不记录按钮位置、字段清单、精确重试序列、逐步回退过程及单次兼容修复细节，这些内容留在代码、测试或 Git 历史中。
- 修复缺陷和调整实现不自动增加文档篇幅。只有用户可见能力、核心流程或安全兼容边界发生变化时，才改写对应说明；默认替换旧内容，不追加补丁式说明。
- 新增句子前判断：删去它是否会影响使用者选择功能，或维护者理解核心实现与风险？若不会，则不写。安全限制、数据存储风险和第三方声明不得为了缩短篇幅而删除。
- 完成后通读两份文档，删除重复和过时内容，检查每个长段落能否继续压缩；除新增独立主要功能或必要边界外，不增加条目或段落。
